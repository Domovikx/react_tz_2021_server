{
  "info": {
    "title": "rest-server"
  },

  "paths": {
    "/api/tasks/all": {
      "get": {
        "description": "Получение всех тасок",
        "responses": {
          "200": {
            "schema": {}
          }
        }
      },

      "post": {
        "operationId": "Create",
        "produces": ["application/json"],
        "responses": {
          "204": {
            "description": "No content"
          }
        },
        "security": [],
        "parameters": [
          {
            "in": "body",
            "name": "body",
            "schema": {
              "properties": {
                "description": {
                  "type": "string"
                }
              },
              "title": "CreateBody",
              "type": "object",
              "required": ["description"]
            }
          }
        ]
      }
    },

    "/api/users": {
      "put": {
        "operationId": "Update",
        "produces": ["application/json"],
        "responses": {
          "204": {
            "description": "No content"
          }
        },
        "security": [],
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "type": "string"
          },
          {
            "in": "body",
            "name": "body",
            "schema": {
              "properties": {
                "description": {
                  "type": "string"
                }
              },
              "title": "UpdateBody",
              "type": "object",
              "required": ["description"]
            }
          }
        ]
      },

      "delete": {
        "operationId": "Remove",
        "produces": ["application/json"],
        "responses": {
          "204": {
            "description": "No content"
          }
        },
        "security": [],
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "type": "string"
          }
        ]
      }
    }
  },

  "produces": ["application/json"],
  "swagger": "2.0",
  "securityDefinitions": {}
}
